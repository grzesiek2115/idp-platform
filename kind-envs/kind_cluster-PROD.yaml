# https://kind.sigs.k8s.io/docs/user/configuration/

kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
name: prod-k8s-cluster
nodes:
- role: control-plane
  image: kindest/node:v1.35.0
  kubeadmConfigPatches:
  - |
    kind: InitConfiguration
    nodeRegistration:
      kubeletExtraArgs:
        node-labels: "ingress-ready=true"
  extraPortMappings:
  - containerPort: 80
    hostPort: 9280
    protocol: TCP
  - containerPort: 443
    hostPort: 9643
    protocol: TCP
- role: worker
  image: kindest/node:v1.35.0

# kind create cluster --config ./kind_cluster-PROD.yaml
# kind delete cluster --name prod-k8s-cluster
